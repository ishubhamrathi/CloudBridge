# Cloud-Agnostic Messaging for Spring Boot

A lightweight Spring Boot starter to build cloud-agnostic applications 
that work across AWS, Azure, GCP, and OCI.

Avoid vendor lock-in by standardizing messaging (SQS, Service Bus, Pub/Sub)
with a unified API.
